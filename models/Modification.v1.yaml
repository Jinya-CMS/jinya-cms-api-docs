title: Modification
type: object
description: Contains the model of modifications that are set during update and create
properties:
  by:
    type: object
    required:
      - artistName
      - email
    properties:
      artistName:
        type: string
      email:
        type: string
        format: email
      profilePicture:
        type: string
  at:
    type: string
    format: date-time
required:
  - by
  - at
